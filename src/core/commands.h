#pragma once

// Command semantics are implemented in Dispatcher.
// This header exists per design-doc structure; logic lives in dispatcher.cpp.

namespace core {
// See dispatcher.h / dispatcher.cpp
}  // namespace core
